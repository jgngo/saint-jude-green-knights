# Cloudflare Pages configuration
name = "saint-jude-green-knights"
compatibility_date = "2025-01-01"

# Build configuration
[build]
command = "pnpm build"
[build.upload]
format = "single_page_app"
directory = "dist"